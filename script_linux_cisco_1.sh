#It is used to list the files and directories within the folder you are currently in.
ls
#This command is used to create a file of a specific size filled with binary zeros. It is the usual first step in creating a swap file or an empty data container.
dd if=/dev/zero of=/tmp/swapex bs=1M count=50